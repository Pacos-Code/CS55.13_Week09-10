rules_version = '2';

// Storage rules: Allow read for all, write only for admin accounts
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read;
      allow write: if firestore.get(
        /databases/(default)/documents/users/$(request.auth.uid)).data.isAdmin;
    }
  }
}
